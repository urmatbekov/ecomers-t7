import React, {Component} from 'react';
import {Container} from "react-bootstrap";

class Cart extends Component {
    render() {
        return (
            <Container>
                <h1>Cart</h1>
            </Container>
        );
    }
}

export default Cart;